% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step_pridit.R
\name{step_pridit}
\alias{step_pridit}
\title{recipes step: PRIDIT composite score}
\usage{
step_pridit(
  recipe,
  ...,
  role = "predictor",
  trained = FALSE,
  score_name = "PRIDIT_score",
  sign_correction = TRUE,
  ecdfs = NULL,
  weights = NULL,
  max_eigval = NULL,
  col_norms = NULL,
  skip = FALSE,
  id = recipes::rand_id("pridit")
)
}
\arguments{
\item{recipe}{A \code{recipe} object.}

\item{...}{One or more selector expressions passed to
\code{recipes::selections()} that identify the numeric indicator columns
to include in the PRIDIT model.}

\item{role}{For the new score column: passed to \code{add_role()}.
Default \code{"predictor"}.}

\item{trained}{Logical.  Set automatically by \code{prep()}; do not change.}

\item{score_name}{Character.  Name of the new score column.
Default \code{"PRIDIT_score"}.}

\item{sign_correction}{Logical.  Passed to \code{\link{pridit}}.
Default \code{TRUE}.}

\item{ecdfs}{Internal.  Stored empirical CDFs from training.}

\item{weights}{Internal.  Stored PRIDIT weight vector from training.}

\item{max_eigval}{Internal.  Stored largest eigenvalue from training.}

\item{col_norms}{Internal.  Stored column norms from training.}

\item{skip}{Logical.  If \code{TRUE}, skip this step during
\code{bake(new_data = NULL)}.  Default \code{FALSE}.}

\item{id}{Character.  Unique step identifier.}
}
\value{
An updated recipe.
}
\description{
Creates a \pkg{recipes} preprocessing step that fits a PRIDIT model on the
training data and appends a single composite score column to any data set
passed to \code{bake()}.  This enables genuine out-of-sample scoring:
the empirical CDFs used for ridit transformation and the PCA weights are
estimated on the training fold only and then applied to the test fold without
re-fitting.
}
\details{
All selected columns must be numeric.  The step does not remove the original
columns; use \code{step_rm()} afterwards if a clean feature set is required.
}
\examples{
\dontrun{
library(recipes)

dat <- data.frame(
  id = letters[1:50],
  x1 = runif(50), x2 = runif(50), x3 = runif(50)
)

rec <- recipe(~ ., data = dat) |>
  update_role(id, new_role = "id") |>
  step_pridit(x1, x2, x3)

prepped <- prep(rec, training = dat)
bake(prepped, new_data = dat)
}

}
